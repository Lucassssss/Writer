#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../phantomjs-pdf/bin/phantomjs-pdf" "$@"
  ret=$?
else 
  node  "$basedir/../phantomjs-pdf/bin/phantomjs-pdf" "$@"
  ret=$?
fi
exit $ret
